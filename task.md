# ai_interviewer 任务清单

更新时间：2026-09-20

## 项目现状

- [x] 需求、UI 原型、系统架构、技术选型、接口与数据模型、P01—P10 输出规范已形成文档。
- [x] Web 单端 M1 Mock 闭环已实现：简历文本 → P01/P02/P03/P04 → 面试 → P06/P07/P08/P09/P10 → 报告。
- [x] 前端主要页面已实现：工作台、简历、准备、流程预览、面试室、复盘、设置、提示词管理。
- [x] 已实现运行期文本模型切换、Mock 语音网关、JSON 文件最小持久化、录音生命周期策略、提示词版本锁定。
- [x] 已实现状态机、评分归一、模式隔离、追问链、趋势数据、错误降级等 M1 关键守卫。
- [x] 当前验证（2026-09-22）：`pnpm run smoke` 通过；contracts 15、server 94、web 20，共 129 个测试通过；`pnpm run smoke:e2e` 在隔离 Mock 服务 27/27 通过；Web 生产构建通过。
- [x] lint 工具链已补齐并通过：ESLint 9 + TypeScript ESLint + React Hooks 规则。
- [ ] 代码知识图谱索引本仓库仍失败（daemon coordination 无法安全确认）；当前使用实际源码、文档、测试和构建结果交叉核对，待环境修复后补建索引。

## P0：先处理的工程问题

- [x] 修复 lint 工具链：补齐 ESLint 依赖、配置和 workspace 脚本，并将 Schema 校验纳入 `smoke`。
- [x] 固化契约产物策略：P01—P10 JSON Schema 纳入版本控制，并由 `pnpm schemas:check` 检查漂移。
- [x] 补齐当前 M1 实际路由的 OpenAPI 契约：见 `docs/openapi.yaml`。
- [x] 清理主要文档状态冲突：需求文档与开发规范已同步到“M1 完成、M2 进行中”。
- [x] 统一 Nest HTTP 异常错误信封，并补充 404/400 回归测试。
- [x] 修复 server e2e 测试文件并行污染：在用户级应用工厂完成前串行运行 server 测试。
- [x] 整理外部对接清单与操作手册：见 `docs/external-integrations.md`。
- [ ] 明确并修复接口文档与实现差异：简历文件导入、鉴权、SSE、幂等、错误信封等目前主要是规范或预留，不能标记为已实现。

## M2：产品化后端基础

### 身份与权限

- [ ] 实现 User / AccountBinding 与 JWT 会话。
- [ ] 实现微信小程序登录、微信网页 OAuth、自建账号兜底；短信登录按供应商方案决定是否进入本阶段。
- [ ] 给所有用户接口增加当前用户解析、资源属主校验和跨用户 403 测试。
- [ ] 给 `/admin/templates` 增加服务端管理员角色守卫、审计记录和权限测试。
- [ ] 统一错误响应为接口规范中的 `{ error: { code, message, details? } }`，覆盖 Nest 默认异常路径。

### 数据与隐私

- [ ] 用 PostgreSQL + Prisma 替换/承接 `InMemoryStore`，完成迁移、枚举、索引和并发更新策略。
- [ ] 将简历、面试、turn、attempt、评价、报告、提示词版本等实体按已确认数据模型落库。
- [ ] 对简历原文、转写、模型配置等敏感数据做加密或密钥托管设计；禁止日志输出全文、token、openid。
- [ ] 实现录音对象存储、session/forever 生命周期、定时清理、删除即弃和孤立对象回收。
- [ ] 落实简历版本、文件大小限制、解析失败状态、删除策略和数据导出/清理策略。

### 简历与文件

- [ ] 实现 `/resumes/import` multipart 上传。
- [ ] 接入 PDF、DOCX、Markdown、TXT 解析；扫描型 PDF 的 OCR 是否支持需单独决策。
- [ ] 将解析任务改为可追踪状态，补充超大文件、损坏文件、空文本和解析超时测试。

## M2：真实 AI 与语音链路

- [ ] 将 MockVoiceGateway 替换为可配置的真实 ASR/TTS 供应商适配器；默认 GLM，支持阿里语音切换和失败回退策略。
- [ ] 完成级联链路 ASR → 文本 LLM → TTS 的真实端到端验证，补齐超时、限流、重试、供应商错误和费用保护。
- [ ] 验证 GLM / DeepSeek / Qwen 真实模型的结构化输出、Schema 校验、repair/retry/degrade 与敏感信息过滤。
- [ ] 确认管理员已发布提示词版本确实进入真实 provider 请求；补充发布、锁定、回滚的集成测试。
- [ ] 评估 Web 端流式转写/反馈的 SSE；端到端实时语音作为独立增强项，不阻塞级联基线。
- [ ] 为模型配置增加密钥加密、脱敏回显、权限边界、连接测试限流和配置审计。

## M2：Web 端与跨端

- [ ] 接入登录态、当前用户、登出、过期恢复和未登录路由保护。
- [ ] 将工作台、历史记录、报告和简历查询改为按用户分页，并实现跨设备恢复。
- [ ] 补齐异常恢复与无障碍：网络失败重试、生成中恢复、录音权限拒绝、键盘导航、读屏标签和移动端边界。
- [ ] 建立 Playwright 或等价浏览器回归，覆盖主要页面和真实 HTTP API。
- [ ] 开始微信小程序端：登录、准备、面试室单列交互、录音上传、报告查看；先做技术 Spike 验证主体、域名和录音限制。

## 质量与发布

- [ ] 建立 CI：typecheck、test、lint、Schema 生成/校验、Web build、HTTP smoke。
- [ ] 将并发测试隔离，避免多个测试进程共享环境状态；保留一次独立的全链路冒烟记录。
- [ ] 增加安全检查：依赖审计、敏感文件扫描、请求体/文件大小限制、CORS 白名单、限流和审计日志。
- [ ] 增加部署配置：生产环境变量、数据库迁移、对象存储、HTTPS、域名、健康检查和备份恢复演练。
- [ ] 在 M2 DoD 中明确：真实模型、登录权限、跨用户隔离、重启恢复、录音生命周期和小程序端分别需要哪些可复现验收证据。

## 建议推进顺序

1. 修复 lint、契约产物和文档状态，确保工程基线可持续验证。
2. 先实现 User/JWT/属主与管理员权限，再迁移 PostgreSQL/Prisma，避免在无权限模型上继续扩功能。
3. 完成真实文件解析和真实 ASR/TTS/LLM 适配，保留 Mock 作为测试 provider。
4. 补齐 Web 登录、跨设备恢复和生产级错误/隐私处理。
5. 以技术 Spike 方式启动微信小程序，验证平台约束后再投入完整页面实现。

## 待用户确认的产品决策

- [ ] 短信登录是否进入 M2，还是仅保留微信登录 + 自建账号。
- [ ] 扫描型 PDF 是否支持 OCR；简历文件和录音的最大大小、保存期限、删除与导出规则。
- [ ] 平台默认模型的费用承担、用户自定义 API Key 的保存方式及额度/限流策略。
- [ ] Web 端是否在 M2 内加入流式转写/反馈；端到端实时语音暂不作为基线。
- [ ] 小程序主体升级企业认证的时间点，以及手机号快捷验证、live-pusher 等能力是否纳入一期。
