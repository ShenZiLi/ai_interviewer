# 可选：接入真实文本模型（OpenAI 兼容：智谱 GLM / DeepSeek / Qwen 均可）
# 设置后服务端默认用真实厂商生成（mode=platform）；未设置则回落到 Mock 样本（mode=mock，无需密钥）。
# 运行期也可在 Web「设置 → 模型配置」里选择厂商预设或填自定义 API 热切换，无需重启。
AI_BASE_URL=
AI_MODEL=
AI_API_KEY=

# 可选：预设里 GLM 默认模型（不填用 glm-4-flash）。
GLM_MODEL=